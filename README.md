# find-out-runtime

download from release (whl package) page:
https://github.com/hylinux/find-out-runtime/releases

and install it in your Python enviroment, after installation completion. you can run it:

find-out-runtime  --cloud china|global --subscription "{your subscription}" --runtime 15.4